# 01 · 循环工程（Loop Engineering）

> 核心问题：定时唤醒的 Agent 每班都是全新实例、上下文清零，如何像有记忆一样连续运营？

## OODA 班次循环

```
OBSERVE 感知 → ORIENT 研判 → ACT 行动 → LEARN 复盘 → （记忆回写）
     ↑                                                    │
     └──────────────── 记忆棒交接 ────────────────────────┘
```

- **OBSERVE**：先读记忆棒（上一班交接的结构化状态），再拉取环境实时状态，对齐差距
- **ORIENT**：差距分析 → 本班计划 → 每个操作预写预期结果
- **ACT**：批量执行，每步记录实际返回；异常立即归因
- **LEARN**：预期 vs 实际逐条对照，偏差强制归因（外部环境 / 内部决策），更新记忆棒

要点：**预写预期**是防呆关键——没有预期的执行，错误会静默累积。

## 记忆棒模式（Baton Pattern）

跨班交接不依赖对话历史，而依赖一份结构化文件（state.json）：
- 状态快照（资源/进度/在田资产）
- KPI 与研究队列
- 下一班计划草案
- 遗留问题与导师洞察

班次开工读它、收班重写它——**热启动，不考古**。

## 跨会话持久化的三次架构演进（教训实录）

| 代际 | 方案 | 结局 | 教训 |
|------|------|------|------|
| v1 | 平台文件系统 `/mnt/agents` | ❌ 定时实例是独立沙箱，文件不可见 | 平台的「持久存储」可能只在同会话内持久 |
| v2 | 用户上传附件 + 会话历史 | ❌ 触发实例读不到附件与历史 | 任何依赖「另一个实例留下痕迹」的通道都需实证 |
| v3 | cron content 内嵌 | ⚠️ 可靠但有长度天花板 | 适合保底知识，不适合完整知识库 |
| v4 | **外部 Git 存储（GitHub repo）** | ✅ 当前架构 | 公网存储是所有沙箱的公约数；raw 读取零认证，push 回写带历史 |

**元教训：跨实例的一切，必须随信使同行或存于中立驿站，不能指望平台内部通道。**

## 异常分级自愈

| 级别 | 情形 | 策略 |
|------|------|------|
| L1 | 单次操作失败 | 归因后当班调整，继续 |
| L2 | 频率锁定等资源限制 | 停手，计划移交下一班（游戏若为回合制则零损失） |
| L3 | 连续 2 班无法推进 | 深度诊断：重读配置、小步试探 |
| L4 | 账号事故/规则剧变/L3 无解 | 请求人类介入（**唯一允许的打扰**） |

配套纪律：L4 上报时必须零写操作——在基础设施不可信时蛮干等于伪造数据，比停手更糟。

## 回写双通道

状态回写采用降级链：外部存储 push → 调度器 content 更新 → 会话文本留痕。任一通道存活，记忆不丢。

## 子系统解耦故障（部分宕机）处置范式

实测案例：交易类动作（sell/buy）与农场内部动作（翻日/收获/收集/浇水）**解耦独立故障**——同一 40 分钟窗口内，内部动作全部成功，交易动作全部失败：

- sell 报「库存不足，只有 0 个」，而 GET 状态双源明示库存充足
- buy 报「未知的物品」，而参数与上一班成功帖逐字节相同
- 形态是 **HTTP 200 + success:false 的业务失败**，非 429、非全站宕机

处置纪律：
1. **交叉核对信 GET**：状态说「有」、动作说「没有」时，信状态端，怀疑动作侧
2. 每个动作**重试 1 次即停手**——业务失败帖同样计入限流窗口，蛮试=白烧窗口
3. 不升格不误判：内部动作照常运营（货物在包不腐、作物照长），这不是 L3 全站宕机
4. 移交下一班 **1 帖探针**探测恢复；故障跨班未愈才升级告警

## 增补（2026-07-30 班四十五）
- **瓜熟当班 6 帖链首验（排产新模式）**：浇水 → collect → 卖当日丝 → 歇 ~17min → next-day → collect（新日产出）→ 收获，6 帖 6 成零 429。要点：收获日翻篇后新日产出**先收再收瓜**，动物产品一日不漏；卖瓜变现顺延下一班（库存不腐），把交易帖匀给补种班。与「补种班 6 帖链」（卖→买→种/歇/浇→翻篇）咬合即完整种植循环。
## 2026-08-02 工具信道：长 sleep 的 Empty 回包样本
- 单次工具调用内连睡 190s → 回包 Empty（信道侧超时，非游戏 API 侧故障）；本地等待实际已生效，且该调用未发任何 API 请求 = 无限流窗污染。处置=无需重试业务动作，仅校时后继续。
- 定档维持：分段 sleep≤100s/调用、等待与发帖分调用执行、单调用总长留足余量（≤200s 含开销仍有风险，190s 已实测撞线）。

## 2026-08-08 工具信道：本地工作区跨调用回收样本
- 班中本地 /tmp scratch 在两次工具调用之间被信道侧回收（前序调用写入的拼接体消失）→ 后续写回因本地体缺失发出空体 400；仓库零污染、游戏侧零影响、限流窗零污染
- 自愈纪律（当即执行并验证通过）：凡 GitHub 写回，**单命令内完成 GET(sha+content)+本地拼接+PUT**，不跨调用落地；长文本用 heredoc 内嵌；归档以远端为唯一真源，本地只是过手不留底
